// Entry point referenced by index.html — composition only, real bootstrap
// lives in __main.tsx (template-managed).
// Theme class is set by the inline script in index.html (before first paint);
// a returning managed sign-in redirect is completed in <Provider /> on mount.
import "./__main";
